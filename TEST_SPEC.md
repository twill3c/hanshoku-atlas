# TEST_SPEC.md — hanshoku-atlas

<!-- scaffold template v1.24.0 から展開(2026-08-31) -->

## CI(`.github/workflows/check.yml`)

push と PR で 3 つのジョブが走る。**ネットワークを要らない構成にしてある** ——
画像の取得も CSV のダウンロードも CI では走らせない(外へ取りに出る CI は「取れなかった日」に落ちる)。

| ジョブ | 中身 |
|---|---|
| `check` | 型検査 / vitest / **about の数字が最新か** / 出荷ビルド / **出荷物に画像が無いこと**(N-03)/ wasm が出荷物にあること |
| `rust` | `cargo test` / wasm32 へのビルド / **いまビルドした wasm でも TS 実装とビット一致すること** |
| `hygiene` | 字種検査(G-10)/ ループログの検証(LL-11) |

**wasm はバイト一致では検査しない。** Rust の版が違えば生成物のバイトは変わるので、
「出荷 wasm とビルド結果が同一バイト」は原理的に達成できない(HC-073)。
見るのは**振る舞い**で、`HANSHOKU_WASM` にビルド結果を渡して同じ照合を回す ——
これが落ちるのは **`rust/src/lib.rs` を直して出荷 wasm を更新し忘れた**ときである。

**`src/data/gates.json` は `tests/gates.test.ts` が書き出す。**
コミットされた値と食い違えば `git diff --exit-code` が落とす ——
`/about` が古い数字を出している状態を、CI が止める。

## 実行規約

- `npm test`(vitest)を stage 3–5 の判定に使用
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(面積比の厳密性・色の分離度)を
  **テスト内の assert で検算**し、導出過程をコメントに残す

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない** |
| 外部権威 | 出典・版・取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す |

## オラクルの出所

| フィクスチャ / オラクル | 出所 | 性格 | 循環していない理由 |
|---|---|---|---|
| `tests/fixtures/color_oracle.json` | **colour-science 0.4.7**(Python・独立実装)が生成 | 外部権威 | 本プロジェクトの TS 実装(Ottosson の OKLab 行列 / CIEDE2000 公式)とコードを共有しない |
| mulberry32 の期待値 | **テスト内で BigInt により独立再計算** | 二経路 | 実装は `Math.imul` と `>>> 0` による 32 ビット演算、テストは `BigInt` の mod 2³² 演算。同じ仕様の別経路 |
| 合成木版の真値(色・面積比) | **生成器が決める** | 構成的 | 抽出器は真値を一切参照しない(G-05・T-016 が静的に検査) |
| TS ↔ Rust/WASM の一致 | **どちらも実装**。期待値は無く、一致するかを測る | 二実装照合 | 一致しうる根拠を先に確かめてある —— WASM 側の演算は加減乗除と比較だけで IEEE-754 が一意に定める。`cbrt` を使う色変換は境界の外(SPEC §2.10 / HC-073) |
| 劣化掃引の測定値 | 実測 | 測定 | ゲートではない。値を記録するだけ(G-03) |

### 判定対象を「標準条件」に移した理由(実測 2026-08-31 / HC-095)

最初は SPEC どおり**無劣化**で測り、ΔE 中央値 0.0000・最大 0.0000、G-目玉1 の的中率 1.000 を得た。
同時に **G-04 の陽性対照が 0/60 で発火しなかった**。抽出器は画素を相異なる色に畳むので、
無劣化では「相異なる色の数 = 版数」となり、**どんな初期化でも各版から 1 点ずつ拾ってしまう**。
つまり無劣化の判定はほぼ恒等式だった。さらに悪いことに、**`chooseK` の符号が逆だった**のに
無劣化では的中率 1.000 に見えていた(慣性が 1e-29 まで落ち、対数が端点を支配するため)。

そこで判定対象を **標準条件(ノイズ σ=2 + クロマ間引き 4:2:0)** に移した。
**閾値は据え置き**で、緩めたのではなく対象を難しくしている。
無劣化は「厳密に 0 でなければ実装に欠陥がある」という床としてのみ残す。

**外部に色オラクルは無い**(SPEC §2.3)。AIC の `color` フィールドは画面の 0.5 % を占める色を
dominant と称し、作品によっては `null` を返す。したがって版色の正解は**自分で構成する**しかない。

## 閾値(SPEC §6 で測定前に宣言済み)

| ゲート | 閾値 |
|---|---|
| G-01 | ΔE2000 の中央値 ≤ 2.0 かつ最大 ≤ 5.0 |
| G-02 | 面積比の絶対誤差 最大 ≤ 0.010 |
| G-06 | 100 回の再実行でビット不一致 0 件 |
| G-目玉1 | エルボー選択規則の的中率(真の N ±1)≥ 0.80 |
| 色変換の一致 | OKLab / Lab 各成分 \|Δ\| ≤ 1e-3、ΔE2000 \|Δ\| ≤ 1e-4 |

**色変換の許容差 1e-3 の根拠:** colour-science は sRGB → XYZ → OKLab の経路を通り、
本実装は線形 sRGB → LMS を Ottosson の行列で直接変換する。**経路が違うので厳密一致はしない**
(白 `#FFFFFF` で colour-science の OKLab L は 1.000001735、本実装は 1)。
達成不能な閾値を掲げないこと(HC-073)。

## ケース一覧

| ID | 対応 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01 | mulberry32 の出力が BigInt による独立再計算と一致する | 先頭 1000 個が完全一致 |
| T-002 | F-01 | sRGB → OKLab がオラクルと一致する(137 色) | 各成分 \|Δ\| ≤ 1e-3 |
| T-003 | F-01 | sRGB → CIELAB(D65)がオラクルと一致する(137 色) | 各成分 \|Δ\| ≤ 1e-3 |
| T-004 | F-01 | ΔE2000 がオラクルと一致する(190 対) | \|Δ\| ≤ 1e-4 |
| T-005 | F-01 | OKLab → sRGB の往復が原色に戻る | 各チャネル \|Δ\| ≤ 1(8 ビット量子化の幅) |
| T-006 | F-02 | 合成木版の面積比が指定どおり | **厳密一致**(画素数で数える) |
| T-007 | F-02 | 無劣化の合成木版は指定色以外の画素を持たない | 相異なる色数 = 版数 |
| T-008 | F-01 | k-means の慣性が反復ごとに単調非増加 | 増加 0 件 |
| T-009 | G-06 | 決定論。同一入力・同一シードで 100 回 | ビット不一致 0 件 |
| T-010 | G-01 | **標準条件**の合成木版 60 枚。k = 真値 のとき版色を復元 | ΔE2000 中央値 ≤ 2.0 / 最大 ≤ 5.0(**実測 0.0000 / 0.3048**) |
| T-011 | G-02 | 同、面積比を復元 | 絶対誤差の最大 ≤ 0.010(**実測 4.43e-4**) |
| T-012 | G-04 | **陽性対照。** 初期化を退化させた抽出器では T-010 が落ちる | 落ちること(**実測 52/60 枚で発火**) |
| T-013 | G-04 | **陰性対照。** 正常な抽出器は 60 枚すべてで通る | 誤検出 0 件 |
| T-014 | G-目玉1 | エルボー選択規則が真の版数を ±1 で当てる(無劣化=宣言どおり) | 的中率 ≥ 0.80(**実測 1.000**) |
| T-014b | G-目玉1 | 同、**標準条件**。UI の主張はこちらに従う | 測定(**実測 1.000**) |
| T-015 | G-03 | 劣化掃引(ノイズ / クロマ間引き / 境界混色 / 地色)の測定 | **閾値なし。値を出力する** |
| T-016 | G-05 | **循環の禁止。** 抽出器のソースが真値・生成器を参照しない | 静的検査 |
| T-017 | F-03 | 版マスクが全画素を過不足なく被覆する | 重複 0・欠落 0 |
| T-018 | F-06 | 地色を除外すると残りの面積比の和が 1 になる | \|Σ − 1\| ≤ 1e-9 |
| T-019 | F-06 | 地色の**提案**は判定ではない —— 提案の的中率を測定として出す | 閾値なし |
| T-020 | F-05 | Met 画像 URL の同一オリジン化。紛らわしいホスト名を巻き込まない | `/met/…` に寄る / 他ホストは不変 |
| T-021 | フッタ規約 | 判定規則の陰性対照 1 + **陽性対照 7**(壊し方ごと)+ 区切り無し実装への耐性 | 規約どおりは通り、**7 通りすべてが落ちる** |
| T-022 | 検品器 | `scripts/*.mjs` の構文検査。走査対象が空でないこと・`node --check` が壊れたソースを落とすこと | 全件通過 / 陽性対照が落ちる |
| T-023 | F-08 | 乱数列が言語をまたいで一致する(k-means++ の初期中心で見る) | 12 標本すべてで一致 |
| T-024 | G-08 | **二実装照合。結論(重心・割当・クラスタ重み・慣性)と経路(反復回数・慣性の履歴・割当変更数の履歴)がビット一致** | 無劣化 / 標準条件 / 退化初期化 / 4 シードで一致 |
| T-025 | G-08 | **陽性対照。**経路だけをずらした実装(収束を 2 回連続で要求)が落ちること、**かつ結論だけを見る照合なら通ってしまう**こと | 12/12 で落ちる / 12/12 が結論は一致 |
| T-026 | F-08 | WASM のポインタを掴み置きしていない(別の大きさを挟んでも結果不変) | 一致 |
| T-027 | F-09 | 同じ摺りを並べれば散らばりは 0 | ΔE も幅も 0 |
| T-028 | F-09 | 版の並び順が変わっても色で正しく結ばれる | 対応が保たれる |
| T-029 | F-09 | 既知のずれを与えるとその大きさが測れる | 与えた量と一致 |
| T-030 | G-09 | **散らばりが表示の桁を決める**。単位 ≤ 幅 < 単位×10 と、桁を 1 つ細かくすると全件で破れること | 表 + 不変量 + 陽性対照 |
| T-031 | F-09 | **本物の版色は対応づけを一意にするほど離れていない**(神奈川沖浪裏の 2 つの藍は ΔE 8.3) | 8 < ΔE < 9 |
| T-032 | G-09 | 10 % 刻みで 0 に丸まる版を「0 %」と書かない | 「< 10 %」 |
| T-033 | F-09 | 怪しい対応(ΔE > 10)を数える。**幅からは外さない** | 件数が返り、幅は縮まない |
| T-034 | F-10 | 幅 1 年の作品はその年にだけ効く | 重み 1 |
| T-035 | F-10 | **幅の広い作品が狭い作品を押しのけない**(重みは 1/幅) | 手計算と一致 |
| T-036 | F-10 | 窓に標本が無ければ **NaN**(0 を返さない) | NaN |
| T-037 | G-目玉2 | 「青」の定義。**露草と藍を分離しないことをテストで固定する** | 境界と射程 |
| T-038 | F-12 | 目盛りの中に入る色名を**全部**返す(一位だけを返さない) | 複数・近い順・目盛り内 |
| T-039 | F-12 | **一位と二位を区別する根拠が無い**ことを本物の版色で示す | 差 < 目盛り幅 |
| T-040 | F-12 | 出典どうしの食い違いを持ち回る | 藍色 14.86 / 片方だけの色は null |
| T-041 | F-13 | 絵師の名寄せ。**末尾の CJK だけを落とす**(ō を消さない)・共同署名は束ねない | 7 組が束なる |
| T-042 | F-12 | **物差しの幅は ⑤ の誤差棒より広い** —— 色名のほうが先に効く | 13.96 > 6.1 |
| T-043 | F-09 | 同一図柄の数を**二つの規則で挟む**(厳しい / 緩い) | 143 / 159 図柄・緩は厳を含む |
| T-044 | F-09 | **厳しい規則は同じ図柄を取りこぼす** —— 神奈川沖浪裏が 3 + 1 に割れる | 4 件が 2 鍵に |
| T-045 | F-09 | **緩い規則は別の図柄を束ねる** —— 叢書名で 14 件 | 14 件 / 厳では複数鍵 |
| T-046 | F-09 | 鍵の作り方(英題を採る・括弧を落とす・空題名は数えない) | 和題の揺れを吸収 |
| T-047 | G-目玉2c' | 対照の指標。**青と赤が重ならない**(対照として成立する条件)・**有彩色は青を含む**(だから 2c'-b は自明でない) | 境界と包含関係 |

## 実ブラウザ検品(`npm run verify:browser`)

テストが緑でも動くとは限らない(HC-062)。**この検品が無ければ、`images.metmuseum.org` が
GET に CORS を返さないことは本番まで分からなかった**(SPEC §2.7 / HC-096)。確かめるのは:

| | 内容 |
|---|---|
| F-05 | 実際に画像を読み、canvas が汚染されずに版色が出る |
| F-03 | 版を押すと canvas が実際に変わる(押せるだけでは意味がない) |
| G-07 | Chromium ↔ Firefox の色相角の差 ≤ 2.0°(**実測 0.000° / 16 進表記まで一致**) |
| N-05 | 390 / 768 / 1280 px で横に溢れない・縦が伸びすぎない(HC-078)、**固定フッタの高さ < body の逃げ** |
| 検品器自身 | 存在しない要素を数えて 0 であること(HC-080 の陽性対照)。失敗は終了コードで返す |
| フッタ規約 | koho-lens 正本の並び。**規則は `scripts/footer-rule.mjs` に切り出し、T-021 で対照つきに検査する** |

### フッタ規約を「・」で分割して数えない(実測 2026-08-31)

区切りの「・」を CSS の `::before` で描いていると **`innerText` に入らない**。
文字列を「・」で分割する実装は、規約どおりの 6 要素を **1 項目**と誤判定した。
正本 `check_footer_dom.mjs` と同じく、**出現順**(`indexOf` / `lastIndexOf`)で照合する。

### 本番検品は「反映の確認」と「検品」を分ける(実測 2026-08-31 / HC-105)

`gh api .../commits/<sha>/status` が `success` を返した後でも、エッジは前の版を配っていることがある。
二度踏んだ —— 直したはずのフッタの逃げが 64px と報告され、置いたはずの `/hanshoku.wasm` が 404 になった。
**同時刻に `curl` した HTML は新しい CSS ハッシュを参照していたのに、ブラウザは古い CSS を受け取っていた。**

`scripts/verify-browser.mjs --url` は検品の前に `waitForSameBuild()` を通す ——
ローカル `out/index.html` の CSS 指紋と本番 HTML の指紋を突き合わせ、
一致するまで最大 10 回・15 秒間隔で待つ。待ちきれなければ**検品とは別の終了コード 3** を返す。
**合格するまで再実行する運用は採らない** —— 反映待ちと本当の不合格が区別できなくなる。

### 固定フッタは折り返して高くなる(実測 2026-08-31)

`position: fixed` のフッタは狭い幅で折り返す。390 px での実高は **89 px** で、
`body` の逃げ(`padding-bottom`)を 64 px にしていたため**最下部の本文が隠れていた**。
横溢れの検査は緑のまま通す種類の故障なので、
**「フッタの高さ < 逃げ」を各幅で確かめる**項目を検品器に置いた。
逃げを 20 px に壊すと 390 px で発火することを確認済み(陽性対照)。

### 存在検査で書いた述語は対象を取り違える(実測 2026-08-31 / HC-098)

初版のホスト検査は「どれかのリンクが `github.com` を向いている」だけを見ていた。
**MIT License のリンク先も `github.com`** なので、「GitHub の行き先を別ホストにする」という
壊し方を落とせなかった —— **陽性対照 7 本のうち 1 本だけが発火しない**という形で露見した。
文言が規約で固定されている項目は、集合にではなく**その項目に対して**検査する。

## ③ の標本は再現できる

`data/frame.json` と `data/sample.json` に**抽出規則と乱数シードを刻んである**。
`MetObjects.csv` の sha256 も入っているので、同じ CSV から同じ 830 件を作り直せる。
版色の抽出は Rust/WASM 実装(TS 実装とビット一致 —— G-08)で、`k=8` / `seed=20260831` 固定。

**取得で 20 件を落としかけた**(17 件はレート制限、3 件は Met 自身の URL に空白)。
`--retry-failed` で拾い直して 830/830。**黙って欠けたままにしない。**

## G-03 —— 模型と実物の両方で測った

L1 の劣化掃引は **JPEG コーデックを通していない**。通しているのは
(a) ガウスノイズ (b) クロマ間引き 4:2:0 (c) 境界の混色(箱平滑) (d) 地色の黄変 の 4 つで、
これは JPEG が平面色の版画に対して起こす主要な効果の**模型**である。

**2026-09-01、実物のコーデックを通して測り直した**(`npm run jpeg`)。
`pipeline/jpeg_codec.py`(Pillow)が符号化と復号だけを担い、合成・抽出・比較は
出荷している実装が行う —— 解析を二度書かない(HC-069)。

**分かったのは、模型が壊れ方を大きく過小評価していたこと。**
模型でのクロマ間引きは最大 ΔE 0.14 でほぼ無害だったが、
**実物の JPEG は品質 95 でも 30 枚中 8 枚で版を潰す**。
しかも壊れ方は品質に単調ではない —— 95 で壊れて 75 で直る標本がある。

**失敗は二種類ある。** 再始動を 4 → 16 に増やすと 8/30 → 4/30 まで減るが、
**64 にしても 4/30 のまま**。半分は探索不足、残り半分は
**慣性がより低い解のほうが真値と合わない**(SPEC §2.17)。

**この測定は CI に載せていない。** Pillow が要るのと、これは**測定であってゲートではない**ため。
結果は `src/data/jpeg.json` に書き出し、`/about` から読む。
