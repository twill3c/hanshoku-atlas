# SPEC.md — hanshoku-atlas(版色アトラス)

<!-- scaffold template v1.24.0 から展開(2026-08-31)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

浮世絵の画像から**版ごとの平面色(版色)を復元し、その分布を測る**。

浮世絵の画像から色相ヒストグラムを出すのは 30 分で書ける。問題はその数字が何を測っているかで、
少なくとも四層の媒介を通っている:(1) 摺られた当時の顔料の色 (2) 200 年超の褪色
(3) 所蔵館の照明・撮影・カラープロファイル (4) 閲覧者のディスプレイ。
制御できるのは 4 だけである。したがって**「浮世絵の色相分布」という対象は素朴には存在しない**。

**本プロジェクトが測るのは「メトロポリタン美術館が公開した画像において、k-means が復元した版色の分布」である。**
この主語を短くしない。誠実さは、不確かさを平滑化して消さず画面に出すことにある。

写真と違い、木版画は一版一色の平面色で構成される。画像の色は連続分布ではなく、
版の数だけの離散クラスタを成している**はず**である。この「はず」が成り立つ範囲を測ることが §5 の目玉になる。

## 2. 底本と経路(実測 2026-08-31)

| 経路 | 実測 | 判定 |
|---|---|---|
| `collectionapi.metmuseum.org` | `Access-Control-Allow-Origin: *` | 使う |
| `images.metmuseum.org`(web-large JPEG) | **GET には `Access-Control-Allow-Origin` を返さない**(6/6)。`curl -I`(HEAD)にだけ返す | 同一オリジンへ書き換えて使う(§2.7) |
| Met 検索 API | `q=Hokusai&hasImages=true` の 58 件中 culture が `Japan*` は **5 件**。`departmentId=6` を足すと総数が 12 に落ちる | **標本枠にしない** |
| `MetObjects.csv` | Git LFS 実体 **317,650,992 B**・CC0 | L4 の標本枠。**リポジトリに入れない** |
| `api.artic.edu` | `ACAO: *`。public domain の和物木版 **6,906 件** | メタデータのみ使う |
| `www.artic.edu/iiif/2/…` | **403 / Cloudflare “Just a moment…”** | **画像は使わない**(§2.2) |
| AIC の `color` フィールド | 神奈川沖浪裏 `percentage: 0.0049`、春信『座敷八景』は `color: null` / `colorfulness: 0` | **色オラクルにしない**(§2.3) |

### 2.1 権利

Met の Open Access は **CC0**(`isPublicDomain: true` の作品)。
**画像を再配布しない** —— URL を参照して閲覧者のブラウザが直接取得する(N-03)。
本プロジェクトが配るのは、そこから計算した数値だけである。

### 2.2 シカゴ美術館の画像は原理的に届かない(実測 2026-08-31)

IIIF エンドポイントは Cloudflare の bot チャレンジを返す。
チャレンジは JavaScript の実行を要求するが、**クロスオリジンの `<img>` / `fetch` はチャレンジを解けない**。
Vercel の datacenter IP を経由するプロキシも同じ判定に当たる公算が高い。

**したがって館間の画像比較はしない。** これは断念ではなく設計変更で、§3 の ⑤ は
**Met 館内の同一図柄・複数摺りの比較**に置き換わる。撮影機材・照明・後処理がほぼ共通になるので、
**差の解釈から撮影条件の交絡が落ちる**。館間比較より方法論的に強い。

### 2.3 外部に色オラクルは無い(実測 2026-08-31)

AIC の `color` は画面の 0.5 % しか占めない色を dominant と称し、作品によっては `null` である。
**色の正解を外部から借りる経路は存在しない。**
したがって正解は**自分で作る** —— 色と面積比を我々が決めた合成木版画像(F-02)。

### 2.4 年代は幅を持つ(実測 2026-08-31)

神奈川沖浪裏は `objectDate: "ca. 1830–32"` に対し `objectBeginDate: 1820` / `objectEndDate: 1842` の **22 年幅**。
Hokusai 標本 58 件のスパン分布は 0 が 22 件、10–35 年が 20 件、150–350 年が 6 件。
**作品を年代に単一配属してはならない**(L4 の F-10)。

### 2.5 同一図柄の複数摺りが Met 内にある(実測 2026-08-31)

題名で引いて実際に確かめた 3 図柄・11 摺り:

| 図柄 | 絵師 | 摺り | 受入番号 |
|---|---|--:|---|
| 神奈川沖浪裏 | 葛飾北斎 | 4 | JP10 / JP1847 / JP2569 / JP2972 |
| 甲州石班沢 | 葛飾北斎 | 3 | JP1327 / JP2986 / JP2581 |
| 大はしあたけの夕立 | 歌川広重 | 4 | JP643 / JP644 / JP2522 / JP3174 |

**「Met に同一図柄の複数摺りが何件あるか」は依然として未測である。**
検索 API は標本枠にならない(§2)ので、数えるのは L4 の `MetObjects.csv` の仕事。
10 題名を引いた時点で 403(レート制限)にも当たった。

### 2.6 画像の実体(実測 2026-08-31)—— stage 1 の断定が二つ外れた

神奈川沖浪裏 4 点 + 広重『東海道五十三次 川崎』(JP3443)を実際に落として測った。

| 版 | 実測 |
|---|---|
| `primaryImageSmall`(web-large) | **600×403 〜 600×459**、95–190 KB、**ICC プロファイル無し(5 点すべて)** |
| `primaryImage`(原寸) | 2000×1350 〜 3912×2995、0.70–3.28 MB。**ICC は JP10 のみ有り(`sRGB.icc` 3136 B)、他 4 点は無し** |

**stage 1 に「1024 px 相当」と書いたのは誤りだった**(web-large は 600 px)。
**「ICC プロファイルの適用差が第五の媒介層になる」という前提も外れた** —— 存在する 1 件は sRGB、
すなわち恒等変換である。外れた予測を消さずここに残す(HC-077)。

**帰結:** 解析の正準画像は **`primaryImageSmall`(600 px)** とする。理由は速度ではなく**均質性**で、
原寸は同じ図柄の 4 摺りの中で ICC の有無が割れており、⑤ の比較にプロファイルの差を持ち込む。
600×403 = 24 万画素あれば、平面色の抽出には足りる。

**G-07 は残す。** 危険が小さいと分かったことは、測らない理由にならない。

### 2.7 Met の画像は直読みできない —— HEAD と GET で応答が違う(実測 2026-08-31)

**stage 2 の実測が誤っていた。**`curl -I` で `Access-Control-Allow-Origin: *` を得たので
「プロキシ不要」と書き、それを根拠に N-01「サーバ処理ゼロ」を立てた。
実ブラウザ検品(Chromium)で **CORS ブロック**され、再実測して分かった:

| 動詞 | ACAO |
|---|---|
| `curl -I`(HEAD) | `*` を返す |
| `curl`(GET) | **返さない(6 回とも)** |

前段の WAF(Incapsula。`visid_incap` の Set-Cookie が付く)が HEAD にだけ別応答をしている。
`collectionapi.metmuseum.org` は GET でも `*` を返すので、**API 側の測定は正しかった**。

**対処:** `vercel.json` の rewrite で `/met/*` を `images.metmuseum.org/*` に書き換え、
ブラウザからは同一オリジンにする。**エッジの書き換えなので関数は起動しない。**
したがって N-01 は「サーバ**関数**ゼロ」と言い直す(§7)。バイトは変換せず中継するだけなので、
測っている対象は「Met が公開した画像」のままである。**画像は保存も再配布もしない**(N-03)。

**検証済み(実測 2026-08-31・本番経路):**
`https://hanshoku-atlas.vercel.app/met/CRDImages/as/web-large/DP141063.jpg` は
**200 / `image/jpeg` / 104,387 B** を返す。Vercel のエッジ(hnd1)から出ても WAF に弾かれない。
実ブラウザ検品も `--url https://hanshoku-atlas.vercel.app/` で通り、
版色・面積比・色相角はローカルと**完全に一致**した(下表)。

| | Chromium | Firefox |
|---|--:|--:|
| 版数 / 相異なる色 | 8 / 55,100 | 8 / 55,100 |
| 抽出 | 920–1,042 ms | 1,180–1,187 ms |
| 慣性曲線(k=2〜16) | 12,142 ms | 16,217 ms |
| 折れ曲がりが示す k | 6 | 6 |
| 色相角の差(両者間) | **0.000°** | 16 進表記まで一致 |

**ローカル検品サーバは書き換えを模倣しているだけ**なので、本番経路の検品は別に走らせること(HC-096)。

```bash
node scripts/verify-browser.mjs --url https://hanshoku-atlas.vercel.app/
```

### 2.8 色相角では褪色する色と残る色を分離できない(実測 2026-08-31)

iro-koyomi の `data/colors/palette.json`(伝統色 369 色)から OKLCh を計算した:

| 色名 | hex | L\* | C | h |
|---|---|--:|--:|--:|
| 露草色(褪せる) | `#21A0DB` | 66.8 | 0.134 | **235.5** |
| 藍色(残る) | `#165E83` | 45.8 | 0.090 | **236.8** |
| 紫(褪せる) | `#A757A8` | 58.2 | 0.148 | 326.8 |
| 藍鼠(褪色後とされる) | `#5E7C85` | 56.6 | 0.037 | 218.1 |
| 紅梅色 | `#E86B79` | 68.2 | 0.155 | 15.2 |
| 弁柄色 | `#8F2E14` | 44.3 | 0.136 | 35.4 |
| 黄土色 | `#C39143` | 69.0 | 0.112 | 76.0 |
| 浅葱色 | `#00A4AC` | 65.4 | 0.111 | 200.9 |

**露草色と藍色の色相角の差は 1.3°。**色相ヒストグラムでは両者を分離できない。分かれるのは明度である。
また褪色は色相環上の移動ではなく、**彩度の崩壊を伴う色相のジャンプ**である(紫 C=0.148 → 藍鼠 C=0.037)。

**帰結:** 分布は色相 1 次元で語らない。(h, C) 平面 + L の層で語る(F-07 / F-10)。
「色相分布」という言葉は、ここで一度手放す。

### 2.9 伝統色名という物差しの目盛り幅(iro-koyomi の実測)

同じ色名の hex が出典間で **ΔE2000 中央値 13.96** ずれている(藍色 14.9 / 藍鼠 20.7 / 紫 24.1)。
**「これは藍鼠」と言い切れる分解能は無い**(F-12)。

### 2.10 二実装は一致するが、速いのは片方だけ(実測 2026-08-31)

k-means の中核を Rust で書き直し、`wasm32-unknown-unknown` に落として TS 実装と突き合わせた。

**一致は完全である。** 無劣化・標準条件・退化初期化・4 種のシードのすべてで、
重心・割当・クラスタ重み・慣性に加えて**経路**(反復回数・各反復の慣性・割当変更数)まで
ビット一致した。経路だけをずらした実装(収束を 2 回連続で要求する)は 12/12 で照合に落ち、
**結論だけを見る照合ならその 12 件とも通ってしまう**ことも実測した(G-08 / HC-065)。

一致しうる根拠は先に確かめてある(HC-073)—— WASM 側の演算は加減乗除と比較だけで、
IEEE-754 が結果を一意に定める。**`cbrt` を使う sRGB → OKLab 変換は境界の外(JS 側)に置いた。**

**速さは一致しない。** 神奈川沖浪裏 JP10(相異なる色 55,100・k=8・再始動 4)で:

| | TypeScript | Rust/WASM |
|---|--:|--:|
| Chromium | 869–1,126 ms | **609–724 ms** |
| Firefox | **968–1,634 ms** | 5,113–6,063 ms |

`opt-level` を `"s"` から `3` に上げても Firefox の傾向は変わらない。
**絶対値は機械の負荷で倍近く振れる**(本番検品では 2 つのブラウザを続けて起動するので、
同じ構成でも Chromium が 610 ms のときと 1,510 ms のときがある)。
**順序は安定しているが、絶対値を一回の測定で代表させない。**
**したがって実行時に、その場で小さな作業を両方に流して速い方を採る。**
ブラウザ名で分岐すると別の版で必ず外す。**この選択が結果を変えないことは G-08 が保証している** ——
等価だからこそ速さだけで選んでよい。どちらを選んだかは画面に出す。

**測り方の注意(HC-104):** 最初のベンチは n=3,000・暖機なしで、Firefox でも「WASM が速い」と出た。
**JIT が効く前の TS と、最初から機械語の WASM を比べていた。**
n=12,000 に上げ、両方を一度空回ししてから測る形に直すと、実画像の順序と一致する。

### 2.11 同じ版から出た色は、思ったより散る(実測 2026-08-31)

⑤ で 3 図柄・11 摺りを測った。**基準の摺りに対し ΔE2000 が小さい対から一対一で結ぶ**という
対応づけのもとでの幅である(対応づけは仮定であって保証ではない)。

| 図柄 | 摺り | ΔE 中央 | ΔE 最大 | 面積比の幅 中央 | 同 最大 | 怪しい対応 |
|---|--:|--:|--:|--:|--:|--:|
| 神奈川沖浪裏 | 4 | **6.1** | 14.1 | 4.3 pt | 23.0 pt | 4 |
| 甲州石班沢 | 3 | **4.4** | 20.7 | 5.9 pt | 17.9 pt | 1 |
| 大はしあたけの夕立 | 4 | **4.6** | 44.3 | 4.5 pt | 14.5 pt | 1 |

**ΔE2000 は 5 を超えれば誰にでも別の色に見える。** 中央値が 4.4〜6.1 ということは、
**同じ版木から出た色の半分が、その域まで散っている**ということである。

「怪しい対応」は ΔE > 10 の対応で、褪色の大きさというより**対応づけが別の版を掴んだ**可能性が高い。
**それでも幅の計算からは外していない** —— 対応がつかないのは「よく分からない」であって
「よく一致した」ではないので、保守側(大きい幅)を採る。

**帰結:** ① の面積比から小数点以下を落とした。3 図柄の中央値の中央値 4.5 ポイントに対し、
「12.3 %」は 0.1 ポイントの分解能を主張してしまう。**1 図柄の幅を代表値にしない。**

### 2.12 標本枠(実測 2026-08-31)

`MetObjects.csv`(317,650,992 B / CC0)を落とし、**構造化列だけ**で絞った。

| 段 | 件数 |
|---|--:|
| 全レコード | 484,956 |
| Department = Asian Art | 37,000 |
| Culture が `Japan` で始まる | 17,034 |
| `Is Public Domain` = true | 15,357 |
| Medium に `oodblock print` | 4,379 |
| 絵本・画帖を除く(`book`)| **3,833 = 枠** |

**年代の分布(中点の 10 年)は 1830s が 637 件で最多**、絵師は広重 584 / 北斎 276 が突出する。
**これが G-目玉2c の対照を要求する理由である** —— 1830 年代に青が増えて見えても、
Met がその時代の風景版画を多く買ったからかもしれない。

抽出は **10 年ごとに最大 45 件・乱数シード 20260831**。標本 830 件のうち
**年代幅 ≤ 10 年は 343 件**(厳密標本)、残り 487 件が幅 10 年超。

画像は `primaryImageSmall`。**幅は 600 px 固定ではない** —— 実測で 127〜600 px の幅があり、
画素数の中央値は 244,800、最小 60,140。60,000 画素未満は版色が潰れるので落とす(宣言値)。

### 2.13 色名の物差しには目盛りが無い(実測 2026-08-31)

⑥ を作って測った結果、**この照合には分解能がほとんど残っていない**。

物差しの目盛り幅は **ΔE2000 = 13.96**(§2.9 —— 伝統色 369 色の、同じ色名の出典間の食い違いの中央値)。
その幅の中に入る色名を数えると:

| | 値 |
|---|--:|
| 目盛りの中に入る色名の数(18 絵師 × 8 版) | **中央値 31 個** |
| 最小 / 最大 | 16 / 60 |

**「これは藍鼠」と言おうとすると、同じ資格の候補が 31 個ある。**
一位が ΔE 2.31 で近くても、その「藍鼠」という名前自体が出典間で **20.68** ずれている。

**物差しのほうが、摺りの散らばりより粗い。** ⑤ の実測(同じ版木から出た色の ΔE 中央値 4.4〜6.1)より
色名の目盛り 13.96 が広い —— **色名で語ろうとすると、摺りの違いより先に物差しの目盛りが効く。**

だから ⑥ は**最も近い一色を当てない**。目盛りの中に入る色名を全部数え、その数を出す。
`data/palette.json` は iro-koyomi 由来で **CC BY-SA 4.0**(コードは MIT)。

### 2.14 絵師の署名は Met の中で揺れている(実測 2026-08-31)

同じ人物が英字だけの版と和名付きの版で二重に入っている。
**末尾の CJK だけを落とす**規則で束ねると、実測で **7 組**が束ねられる
(春章 / 北斎 / 是真 / 俊満 / 辰斎 / 国貞 / 貞信)。
**非 ASCII をすべて落とす実装は `Shunshō` の `ō` まで消した** —— 範囲は CJK に限る。

**共同署名(`|` を含む)は束ねない。** 彫師と絵師のどちらが先に来るかが一定でなく、
「主たる絵師」を機械的に決める規則を作れない。7 署名 / 12 作品が該当する。

**帰結:** 標本 830 件で 12 件以上ある絵師は **18 名(507 件)**。
写楽 5 / 国芳 7 / 芳年 9 件で、構想が挙げていた七人のうち三人は棚に載らなかった。
**載らなかったことを書く。**

### 2.15 /about の数字は、測定が書き出す(2026-08-31)

**手で写した数字は必ずずれる**(HC-045)。`/about` が出す値は一つも手書きしていない。

| 数字 | 書き出すもの | ずれたら |
|---|---|---|
| G-01 / G-02 / G-04 / G-目玉1 の実測 | `tests/gates.test.ts` → `src/data/gates.json` | テストを走らせれば上書きされる |
| ⑤ の図柄ごとの幅 | `scripts/verify-browser.mjs --write-spread` → `src/data/spread.json` | **毎回の検品が食い違いを落とす** |
| ③ の判定と 10 年ごとの値 | `pipeline/aggregate.mjs` → `src/data/bands.json` | 集計を回せば上書きされる |
| ④/⑥ の色名の数 | `pipeline/artists.mjs` → `src/data/artists.json` | 同上 |
| 標本の件数・k・シード | `pipeline/analyze.mjs` → `src/data/plates-meta.json` | 同上 |

とくに ⑤ は**検品が現物と突き合わせる**ようにした ——
`src/data/spread.json` が実測と違えば `検品 NG` になる。

## 3. 画面

| | 画面 | 計算 | ループ |
|---|---|---|---|
| ① | 一枚解剖 —— 版色の抽出・面積比・OKLCh・版のマスク | 実行時 | L1 |
| ② | 色相環 —— (h, C) 平面への布置 | 実行時 | L2 |
| ③ | 年代の帯 —— 1760s–1900 の分布遷移(`/nendai/`) | 事前 | L4 |
| ④ | 絵師くらべ —— **標本に 12 件以上ある 18 名**の色域の形(`/eshi/`) | 事前 | L5 |
| ⑤ | 摺りの散らばり —— 同一図柄の複数摺り(`/suri/`) | 実行時 | L3 |
| ⑥ | 伝統色との照合 —— iro-koyomi 接続(`/eshi/`) | 事前 | L5 |
| — | `/about` —— 測れなかったことの一覧 | 事前 | L6 |

## 4. 機能要求

| ID | 要求 | 優先度 | ループ |
|---|---|---|---|
| F-01 | 版色抽出器。OKLab 上の k-means(k-means++ 初期化・**固定シードで決定論的**)で版色・面積比・OKLCh を返す | must | L1 |
| F-02 | 合成木版生成器。**色と面積比が既知**の N 版画像を生成し、劣化(ノイズ・クロマ間引き 4:2:0・境界混色・地色の黄変)を段階的に加えられる | must | L1 |
| F-03 | ① 一枚解剖。画像 URL を入力 → k スライダー → パレット・面積比・OKLCh・**版のマスク表示** | must | L1 |
| F-04 | 全画面に**測っている対象の主語**を出す(§1 の一文を短くしない) | must | L1 |
| F-05 | Met の **`primaryImageSmall`** を**同一オリジンに書き換えて**読み、canvas を汚さずに `getImageData` する(§2.6 / §2.7) | must | L1 |
| F-06 | **紙の地色を版色に数えるかのトグル。** 既定は「数える」。分析者の判断が入る場所を隠さない | must | L1 |
| F-07 | ② 色相環。角度=h、半径=C、点の大きさ=面積比、**L は点の縁で表す**(§2.8 により明度で潰さない) | must | L2 |
| F-08 | Rust/WASM 実装と TS 実装の二実装照合(F-01 の同一性)。**実行時はその場で速い方を測って選ぶ**(§2.10) | should | L2 |
| F-09 | ⑤ 摺りの散らばり。Met 館内の同一図柄・複数摺りの版色を並べ、**実測の散布幅**を出す(§2.11) | must | L3 |
| F-10 | ③ 年代の帯。**begin–end に一様配分した重み付き分布**。厳密標本(幅 ≤ 10 年)と緩標本(全件)を並走 | must | L4 |
| F-11 | 標本枠。`MetObjects.csv` の構造化列から層別抽出(年代 × 絵師)。**抽出規則と乱数シードをファイルに刻む** | must | L4 |
| F-12 | ⑥ 伝統色照合。**候補を複数列挙**し、§2.9 の目盛り幅を同時に描く(§2.13) | should | L5 |
| F-13 | ④ 絵師くらべ(`/eshi/`)。**絵師の版色をもう一度 k-means にかける** —— 一段目は 1 枚の中の版、二段目は 1 人の中の版 | should | L5 |
| F-14 | `/about` —— 四層の開示、**落ちたゲートの記録**、測って捨てた設計の記録。**数字は手で書かず、測定が書き出したファイルから読む**(§2.15) | must | L6 |

## 5. 目玉 —— 木版に対する k-means は制作構造の推定になるか

写真に対する k-means は便宜的な減色でしかない。木版画に対するそれは、**一版一色という物理的制作構造の推定**でありうる。
成り立つなら、k を動かして残差を見ることは「この絵は何版で摺られたか」の推定になる。

**この主張は L1 で判定する。** 判定は合成木版(F-02)に対して行う ——
**本番作品の版数はカタログに書かれていない**(Met の medium 欄は “Woodblock print; ink and color on paper” までで版数を記さない)。
正解が無いものを本番データでゲートにはできない(HC-002)。
**したがって G-目玉1 は本番作品では検証されない。この限界は `/about` に書く。**

### 5.1 G-目玉2 の宣言(2026-08-31・**一枚も測る前に書いた**)

主張は「**1829–30 年前後に、青の面積比が非連続に増える**」。ベロ藍(プルシアンブルー)の普及が
風景版画というジャンルを生んだ、という通説が数字に出るかを見る。

**「青」の定義。** OKLCh の色相角 h ∈ [200°, 270°) かつ彩度 C ≥ 0.02。
**これは「青系全体」であって「ベロ藍」ではない** —— §2.8 の通り、露草色(h=235.5°)と
藍色(h=236.8°)は色相角で 1.3° しか離れておらず、この指標は両者を分離しない。
**主張はもともとこの分だけ弱い。**

**年代の扱い。** 作品を年代に単一配属しない(§2.4)。`objectBeginDate`–`objectEndDate` に
一様配分した重み付きで年ごとの平均を出す。標本は二本並走させる ——
**厳密標本**(年代幅 ≤ 10 年)と**緩標本**(全件)。

| ID | 判定 | 閾値 |
|---|---|---|
| G-目玉2a | 青の面積比の年平均が 1820–1828 → 1831–1840 で増える | **+3.0 ポイント以上**。厳密標本・緩標本の**双方で** |
| G-目玉2b | その増加が**非連続**である | 2a の差が、同一標本の「1800–1818 → 1819–1828」の差と「1841–1850 → 1851–1860」の差の**どちらよりも大きい** |
| G-目玉2c | **標本の偏りで説明できない** | 遷移をまたぐ絵師に限った部分標本でも 2a の符号が同じ |

**どれか一つでも落ちたら、件数による主張を破棄して実例列挙に格下げする。閾値は緩めない。**

#### 判定(実測 2026-08-31・標本 830 件)

| | 実測 | 判定 |
|---|---|---|
| 2a | 緩標本 **+2.20 pt** / 厳密標本 +4.36 pt | **不通過**(緩標本が閾値 +3.0 に届かない) |
| 2b | 緩標本の対照は 前 +2.61 / 後 −1.23。**2.20 < 2.61** | **不通過** |
| 2c | 遷移をまたぐ絵師 **2 名**・作品 62 件・差 +8.13 pt | 通過。**ただし通過に意味がない**(下記) |

**G-目玉2 は不通過。宣言どおり格下げした。閾値は一切動かしていない。**

**落ち方に理由がある。** 厳密標本だけを見れば 2a も 2b も通る(対照は +0.46 と −6.69)。
落としたのは緩標本で、そこでは 1820–1828 の青が 3.41 pt ある —— 10 年ごとに数えれば
1820 年代は 1.68 % しかない。差は**年代幅の広い作品が 1830 年代の青を 1820 年代へ滲ませている**
ことから来る。**二本並走させたのは、まさにこれを見るためだった。**

**2c の対照は退化していた。** 条件(1828 以前 3 件以上 かつ 1831 以降 3 件以上)を満たした絵師は
2 名だけで、一方は `Unidentified artist`、他方は **1764 年に没した奥村政信**である
(「1831 年以降の作品」は年代幅の広い作品による見かけ)。
**判定は通過と出たが、通過を根拠にしない。**「対照が通った」ことと「対照が何かを言った」ことは別である。

#### 格下げ後に置いたもの —— 10 年ごとの観測(主張ではない)

| 10 年 | 標本 | 青の面積比の平均 |
|---|--:|--:|
| 1700s–1790s | 各 9〜45 | **0.00 %**(1730s の 0.11 % を除き、九つの 10 年すべて) |
| 1800s / 1810s | 45 / 45 | 0.31 % / 0.10 % |
| 1820s | 45 | 1.68 % |
| **1830s** | 45 | **7.08 %** |
| 1840s / 1850s / 1860s | 45 | 3.58 % / 5.58 % / 2.73 % |
| 1870s 以降 | 45 | いずれも 1 % 未満 |

**これは観測であって因果ではない。** Met がその時代の風景版画を多く持っているだけかもしれず、
2c はそれを排除できなかった。

**あらかじめ分かっている交絡。** Met の収蔵は Met の収集史であって浮世絵の母集団ではない。
1830 年代に青が増えて見えたとして、それは顔料史かもしれないし、
**Met が北斎・広重の風景版画を多く買ったから**かもしれない。2c はその対照である。

## 6. 品質ゲート

**閾値は測る前に宣言する。落ちたら閾値を緩めず、主張の側を格下げする。**

| ID | 内容 | 閾値(宣言 2026-08-31) | 落ちたときの退避先 | ループ |
|---|---|---|---|---|
| G-01 | 無劣化の合成木版に対し、k=真値 のとき版色を復元する | ΔE2000 の**中央値 ≤ 2.0 かつ最大 ≤ 5.0** | 抽出器の欠陥として扱い、直すまで進まない | L1 |
| G-02 | 同、面積比を復元する | **絶対誤差の最大 ≤ 0.010** | 同上 | L1 |
| G-03 | **劣化耐性の測定。** ノイズ・クロマ間引き 4:2:0・境界混色・地色の黄変を段階的に加え、G-01/G-02 がどこで落ちるかを記録する。**L1 は JPEG コーデックを通さない** —— 通すのは平面色に対する JPEG の主要効果の**模型**であり、「品質 75 で壊れる」とは言えない(実コーデックは L2) | **閾値を置かない。壊れる境界を測って書く** | —(測定であってゲートではない) | L1 |
| G-04 | **陰性対照。** 抽出器の初期化を壊した版(k-means++ を退化させる)で G-01 が落ちる | 落ちること | 検査が働いていないので、検査ごと捨てる | L1 |
| G-05 | **循環の禁止。** 抽出器は真値を参照していない | 静的に確認 | 該当検査を無効と判定 | L1 |
| G-06 | **決定論。** 同一入力・同一シードで抽出結果がビット一致する | 100 回で不一致 0 件 | シード管理の欠陥として直す | L1 |
| G-目玉1 | **エルボーが版数を当てる。** 無劣化合成木版で、既定の選択規則が真の N を ±1 以内で当てる | **的中率 ≥ 0.80** | **「版数の推定」を撤回**し、k は分析者が選ぶ道具として提示する。① のスライダーは残る | L1 |
| G-07 | ブラウザ色管理。Chrome / Firefox で同一画像の版色の色相角が一致する(§2.6 により通る見込みだが**測る**) | **Δh ≤ 2.0°** | **ブラウザ差を画面に表示する。** 隠して平均しない | L1 |
| G-08 | 二実装照合(TS ↔ Rust/WASM)。結論だけでなく**経路**(反復回数・各反復の割当変化数)も比べる | 完全一致 + 経路をずらした陽性対照が落ちる。**実測: 無劣化/標準条件/退化初期化/4 シードのすべてでビット一致、陽性対照は 12/12 発火** | 一致させる規則を分離して書き直す | L2 |
| G-09 | 誤差棒。①〜④ の主張が ⑤ の実測散布幅より細かくない | **最後に表示する桁は、散らばりが飲み込まない最初の桁**(単位 ≤ 幅 < 単位×10)。実測を受けて ① の面積比は**小数点以下を落とした** | 主張の桁を落とす | L3 |
| G-目玉2 | 1829–30 年前後に青の面積比が非連続に増える。**厳密標本と緩標本の双方で** | §5.1 に宣言(2026-08-31・測定前) | 件数による主張を破棄し、**実例列挙**に格下げ | L4 |
| G-10 | 字種検査。日本語本文に別字種・制御文字が混入しない | `harness/text_hygiene.py` が 0 件 | 直す | 全 |

## 7. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | **サーバ関数ゼロ。** Next.js 静的書き出し(`output: "export"`)のみ。API Route / Edge Function を持たない。**画像は `vercel.json` の rewrite で中継する**(エッジの書き換えであって関数ではない — §2.7) | `next build` の出力に関数が無いこと |
| N-02 | **課金経路ゼロ。** ビルド時に外部へ取りに行かない。実行時の外部取得は閲覧者のブラウザ ↔ Met のみ | ビルドをネットワーク無しで通す |
| N-03 | **画像を再配布しない。** URL 参照のみ。リポジトリにも `out/` にも Met の画像を置かない | ビルド成果物の走査 |
| N-04 | ① の抽出は **web-large**・k ≤ 16 で 1 秒以内(Web Worker)。**安定しては未達**(実測 2026-08-31・L2 後)—— 複数回の実測で Chromium **610〜1,510 ms**、Firefox **968〜1,432 ms**。速い回は 1 秒を切るが、**計測機の負荷で倍近く振れる**ので「1 秒以内」とは言えない。1 回の良い測定を代表値にしない | 本番 URL への実ブラウザ検品 |
| N-06 | 慣性曲線(k=2〜16)は**押したときだけ**走らせる。実測 **12.2〜14.7 秒**で、自動実行すると画面が固まる | 実ブラウザ実測 |
| N-05 | モバイル対応。複数の画面幅で実際に見る(HC-078) | 実ブラウザ検品 |
| N-07 | **フリート共通のフッタ規約**(koho-lens が正本)—— MIT License ・ © ・ GitHub ・ 歩き方 ・ 設計図 ・ App Menu を この並びで `position: fixed`。文言は各アプリのものを残す | `scripts/footer-rule.mjs` を実ブラウザの DOM に当てる(T-021 で規則自体を対照つきに検査) |

## 8. スコープ外

- **館間の画像比較**(§2.2)。他館は参照リンクに留める
- **ukiyo-e.org による館間名寄せ。** ⑤ を館内比較にしたので不要になった
- **褪色前の推定色の点推定。** 逆算モデルは出力ではなく、⑤ の実測散布を説明できるかで**検証される側**に置く。
  点推定を出すことは、§1 で立てた方針と正面から矛盾する
- **版数の本番作品での検証**(§5)。合成データに対してのみ判定する
- **原寸画像の取得。** 色分析には 600 px で足り、かつ原寸は ICC の有無が作品ごとに割れている(§2.6)
